# 按鍵組合與微軟注音對齊分析（分析,尚未實作）

本文盤點目前所有按鍵組合、比對微軟注音的實際慣例,並標出分歧。**不含任何實作**;
其中多項牽涉 `AGENTS.md` 的受保護契約,需明確決定後才能動工。

## 1. 核心錯誤

微軟注音的修飾鍵分工是:

- **Ctrl + 按鍵 → 中文全形標點**
- **Shift + 按鍵 → 標準 ASCII 字元**

我們做反了:把中文標點掛在 Shift 上,Ctrl 幾乎完全放行。這造成一個直接可觀察的
不一致 —— 同一個實體按鍵,注音模式按 `Shift+,` 得到「,」,按 Shift 切到英文模式
後按 `Shift+,` 卻得到 `<`。使用者對同一組按鍵得到兩種語意。

微軟的 Ctrl 標點快速鍵**在中文與英文模式下都有效**,不需要切換模式就能輸入標點,
這正是它比 Shift 方案好的地方。

## 2. 我們目前的設計（以程式碼為準）

### 修飾鍵分派順序（`filterKeyDown`）

| 順序 | 條件 | 行為 |
|---|---|---|
| 1 | 按住 Alt | 全部放行給應用程式 |
| 2 | 按住 Ctrl | 放行,**唯一例外**是候選開啟時的 `Ctrl+PageUp`（固定候選） |
| 3 | 英文模式 | 全部放行 |
| 4 | 數字鍵盤 | 直接輸出 ASCII |
| 5 | 候選開啟 + 數字鍵 | 選字 |
| 6 | **Shift + 標點/數字** | **輸出全形中文標點** |
| 7 | Shift + A-Z | 暫時輸出大寫英文字母 |
| 8 | Space / 方向鍵 / Enter / Backspace / Esc | 組字控制 |
| 9 | 其他 | 查注音對應 |

模式切換:**單獨輕點 Shift** 切換中/英。

### 目前的 Shift 標點表（`SHIFT_PUNCTUATION`）

| 按鍵 | 我們輸出 | 微軟(Shift 時)應為 |
|---|---|---|
| Shift+Space | `ˉ` 一聲符號 | **全形/半形切換**（已決定刪除 `ˉ`,但不實作切換） |
| Shift+1 | `！` | `!` |
| Shift+2 | `＠` | `@` |
| Shift+3 | `＃` | `#` |
| Shift+4 | `＄` | `$` |
| Shift+5 | `％` | `%` |
| Shift+6 | `……` | `^` |
| Shift+7 | `＆` | `&` |
| Shift+8 | `＊` | `*` |
| Shift+9 | `（` | `(` |
| Shift+0 | `）` | `)` |
| Shift+; | `：` | `:` |
| Shift+= | `＋` | `+` |
| Shift+, | `，` | `<` |
| Shift+- | `——` | `_` |
| Shift+. | `。` | `>` |
| Shift+/ | `？` | `?` |
| Shift+` | `～` | `~` |
| Shift+[ | `『` | `{` |
| Shift+\ | `｜` | <code>&#124;</code> |
| Shift+] | `』` | `}` |
| Shift+' | `「` / `」` 交替 | `"` |

### 未加修飾鍵時的注音對應（大千,正確,不需更動）

`,`→ㄝ　`.`→ㄡ　`/`→ㄥ　`;`→ㄤ　`-`→ㄦ
`1`→ㄅ　`2`→ㄉ　`3`→ˇ　`4`→ˋ　`5`→ㄓ　`6`→ˊ　`7`→˙　`8`→ㄚ　`9`→ㄞ　`0`→ㄢ
`'` `[` `]` `=` `\` `` ` `` 無注音對應（目前閒置）

## 3. 微軟注音的實際慣例

來源見文末。兩個獨立來源的標點表一致。

| 標點 | 微軟快速鍵 |
|---|---|
| `，` | Ctrl + `,` |
| `。` | Ctrl + `.` |
| `、` | Ctrl + `'` |
| `；` | Ctrl + `;` |
| `：` | Ctrl + Shift + `;` |
| `？` | Ctrl + Shift + `/` |
| `！` | Ctrl + Shift + `1` |
| `【` `】` | Ctrl + `[` / Ctrl + `]` |
| `｛` `｝` | Ctrl + Shift + `[` / Ctrl + Shift + `]` |

其他慣例:

| 功能 | 微軟按鍵 | 我們現在 |
|---|---|---|
| 中/英模式切換 | Ctrl + Space | 輕點 Shift |
| 全形/半形切換 | Shift + Space | 無（Shift+Space 被佔用為 `ˉ`） |
| 符號小鍵盤 | Ctrl + Alt + `,` | 無 |
| 符號前導字元 | `` ` `` 開頭 | 無（`` ` `` 閒置） |
| Shift + 任意鍵 | 標準 ASCII | 全形中文標點 |

## 3.5 已決定事項

| 項目 | 決定 |
|---|---|
| Shift+Space 的 `ˉ` 一聲符號 | **刪除**。不另尋按鍵,直接移除該功能 |
| 全形符號 `＠＃＄％……＆＊` 等 | **不保留全形版**。Shift+對應鍵就是標準 ASCII |
| 符號小鍵盤（Ctrl+Alt+`,`）與 `` ` `` 前導輸入 | **不做** |
| Ctrl 標點是否攔截應用程式快速鍵 | **攔截**。使用者不需要 `Ctrl+,` / `Ctrl+.` 的編輯器功能 |

一次按鍵只能有一個接收者:輸入法位於鍵盤與應用程式之間,取用了該按鍵,應用程式
就收不到。這不是可以兩全的取捨。備選方案是「只在注音模式攔截,英文模式放行」,
讓使用者切到英文即可取回應用程式快速鍵 —— 比微軟細緻,但偏離純微軟行為。目前
採用微軟做法（兩種模式都攔截）。

### 決定「Shift 回歸 ASCII」的實作陷阱

單純刪掉 `SHIFT_PUNCTUATION` 是不夠的,會產生更糟的結果。`filterKeyDown` 的最後
一行是 `symbol_for_event(keyEvent.keyCode, keyEvent.charCode)`,它只看**實體按鍵**:
`Shift+2` 的 keyCode 仍是 `0x32`,對應到 `2`,查表得到注音 `ㄉ`。也就是說刪表之後
`Shift+2` 不會變成 `@`,而會變成 `ㄉ`。

因此必須明確處理:**按住 Shift 且非 A-Z 時一律放行**,讓 Windows 送出正常的
shifted ASCII 字元。

另有一個順序問題需一併處理。現行契約要求「Shift 標點只取代未完成的音節,保留
已完成的中文,並在游標位置輸出」。改成放行後,應用程式會直接收到字元,而我們
未提交的組字內容仍懸在那裡。放行前必須先提交組字,否則字序會錯亂。

## 4. 分歧清單與待決事項

### A. 標點改掛 Ctrl（本次的主因）

把 `SHIFT_PUNCTUATION` 整張表移到 Ctrl,並讓 Shift+按鍵回歸 ASCII。

**需要決定的風險**:Ctrl 組合會與應用程式快速鍵衝突。`Ctrl+,` 在 VS Code、
Visual Studio 等是「設定」,`Ctrl+.` 在許多編輯器是「快速修正」。微軟注音選擇
攔截它們;我們目前是完全放行。攔截後,這些應用程式的原有快速鍵在中文輸入法下
會失效。微軟的做法是一致的,但這是行為改變,需要你確認要不要照做。

### B. Shift+Space 的歸屬（已決定:刪除 `ˉ`）

`ˉ` 一聲符號直接移除,不另尋按鍵。Shift+Space 因此空出。

全形/半形切換本身**不實作**（屬第 E 項「不做」的範圍）,空出的 Shift+Space 單純
放行給應用程式。日後若要補上全形/半形,位置已經是對的。

注意:其餘三個聲調鍵 `3=ˇ 6=ˊ 4=ˋ 7=˙` 在無作用中音節時仍輸出聲調符號,**不受
本項影響**;只有 `ˉ` 消失。這會造成四聲有符號、一聲沒有的不對稱,是此決定的
已知後果。

### C. 模式切換鍵

我們用輕點 Shift,微軟用 Ctrl+Space。輕點 Shift 是其他中文輸入法（如新酷音）的
常見做法,也在 `AGENTS.md` 受保護。

**建議**:保留輕點 Shift,額外支援 Ctrl+Space。兩者不衝突。**待你確認。**

### D. 全形符號（已決定:不保留）

`＠＃＄％……＆＊＋——～｜『』` 等全形符號全部取消,`Shift+對應鍵` 就是標準
`@#$%^&*+_~|{}`。這些字元若真的需要,使用者仍可透過系統的表情符號/符號面板
（Win+`.`）輸入,不需輸入法提供。

`Shift+'` 目前的 `「` / `」` 交替引號一併取消,回歸 `"`。引號改由 Ctrl+`[` `]`
提供（見第 4.5 節）—— 交替式輸入本身也不理想:它依賴一個隱藏的開闔狀態,
使用者若在中途插入或刪除引號,狀態就會與文字不同步。改成左右各自獨立的按鍵
後,輸出永遠可預期。

另外,目前 `Shift+[` `]` 輸出的是 `『』`（U+300E/U+300F,白角括號,空心外觀）,
而非常用的 `「」`（U+300C/U+300D）。新設計已修正這個對調。

### E. 尚未擁有的微軟功能（已決定:不做）

符號小鍵盤（Ctrl+Alt+`,`）與 `` ` `` 前導輸入法**本次不實作**。因此沒有直接
快速鍵的全形符號不會有輸入法內的入口,這是 D 項決定的已知取捨。

### F. 既有的產品專屬鍵

`Ctrl+PageUp` 固定候選是本產品自有功能,微軟沒有。改用 Ctrl 做標點後兩者不衝突
（PageUp 不在標點表中）,建議保留。

## 4.5 目標設計（定稿,待實作）

### 修飾鍵分派順序（取代現行 `filterKeyDown` 的前段）

| 順序 | 條件 | 行為 | 與現況比較 |
|---|---|---|---|
| 1 | 按住 Alt | 放行 | 不變 |
| 2 | 按住 Ctrl 且命中標點表 | 輸出中文標點,**中英模式皆生效** | **新增** |
| 3 | 候選開啟時 `Ctrl+PageUp` | 固定候選 | 不變 |
| 4 | 其餘按住 Ctrl | 放行 | 不變 |
| 5 | 英文模式 | 放行 | 不變 |
| 6 | 數字鍵盤 | 輸出 ASCII | 不變 |
| 7 | 候選開啟 + 數字鍵 | 選字 | 不變 |
| 8 | Shift + A-Z | 暫時大寫英文 | 不變 |
| 9 | **Shift + 非字母** | **先提交組字,再放行** | **改變**（原為全形標點） |
| 10 | 其他 | 查注音 | 不變 |

順序 2 必須排在順序 5「英文模式放行」之前,否則 Ctrl 標點在英文模式下會失效。

### Ctrl 標點表（取代 `SHIFT_PUNCTUATION`）

| 按鍵 | 輸出 |
|---|---|
| Ctrl + `,` | `，` |
| Ctrl + `.` | `。` |
| Ctrl + `'` | `、` |
| Ctrl + `;` | `；` |
| Ctrl + Shift + `;` | `：` |
| Ctrl + Shift + `/` | `？` |
| Ctrl + Shift + `1` | `！` |
| Ctrl + `[` | `「` |
| Ctrl + `]` | `」` |
| Ctrl + Shift + `[` | `『` |
| Ctrl + Shift + `]` | `』` |

**此處刻意偏離微軟**:微軟把 `【` `】` 放在 Ctrl+`[` `]`,把 `｛` `｝` 放在
Ctrl+Shift+`[` `]`。但正體中文最常用的引號是 `「」`,`【】` 相對罕用,而使用者
明確表示需要 `「」`。因此改為外層引號 `「」` 走 Ctrl,內層引號 `『』` 走
Ctrl+Shift —— 與「Shift 代表次要變體」的直覺一致。

代價:`【】` 與 `｛｝` 在輸入法內沒有入口,需要時以系統符號面板（Win+`.`）輸入。

### 移除項目

- `SHIFT_PUNCTUATION` 整張表
- `Shift+Space` → `ˉ`
- `Shift+'` → `「` / `」` 交替,連同 `self.quote_open` 狀態一併移除

### 明確維持不變

輕點 Shift 切換中英、Shift+A-Z 暫時英文、數字鍵盤一律 ASCII、無音節時
`3=ˇ 6=ˊ 4=ˋ 7=˙`、十項兩欄候選頁、方向鍵翻頁與右側編輯、`Ctrl+PageUp` 固定候選。

### 模式切換（已決定）

**不加** `Ctrl+Space`。維持輕點 Shift 為唯一的中英切換方式。

## 5. 契約影響

`AGENTS.md` 中下列條文會因對齊而需要修改,**這些是你的標準指示,我不會自行變更**:

- 「Shift punctuation ... are core behaviors. Do not replace or remove these
  behaviors without an explicit user request.」
- 「Shift+A-Z and Shift punctuation replace only an unfinished active syllable,
  preserve completed Chinese, and emit at the active caret.」（Shift+A-Z 的部分
  不受影響,僅標點部分需改寫）
- 「With no active syllable, bare DaQian tone keys emit their tone marks
  (3=ˇ, 6=ˊ, 4=ˋ, 7=˙), while Shift+Space emits ˉ.」（Shift+Space 部分）

未受影響、應維持不變的契約:單獨輕點 Shift 切換模式、Shift+A-Z 暫時英文、
數字鍵盤一律輸出 ASCII、十項兩欄候選頁、方向鍵翻頁與編輯。

## 6. 尚待你確認的事實

以下我以查到的資料為準,但未在本機實測微軟注音,建議你順手驗證:

1. 微軟注音在**中文模式**下,`Shift+,` 是否確實輸出 `<`（而非全形）。
2. `Ctrl+Shift+1` 是否確實輸出 `！`（我們現在是 `Shift+1`）。
3. `Ctrl` 標點是否在英文模式下也有效。

## 來源

- [微軟注音的標點符號快速鍵 | Wen Taichi](https://wentaichi.me/life/punctuation-mark-shortcut-in-microsoft-bopomofo)
- [Windows 標點符號輸入方法教學,快速鍵整理 | G. T. Wang](https://blog.gtwang.org/windows/windows-punctuation-input-method-shortcut-keys/)
- [【Windows】如何用快捷鍵切換全/半形](https://support.iqt.ai/hc/zh-tw/articles/4465670983577--Windows-%E5%A6%82%E4%BD%95%E7%94%A8%E5%BF%AB%E6%8D%B7%E9%8D%B5%E5%88%87%E6%8F%9B%E5%85%A8-%E5%8D%8A%E5%BD%A2)
